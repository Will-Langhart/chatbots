# models/openai_api.py

def get_response(message):
    """
    Simulate an API call to OpenAI to generate a response.
    In a real-world implementation, you would use the OpenAI SDK.
    """
    # For demonstration, simply echo the message.
    return f"Echo: {message}"
